<Window x:Class="EchoUI.Views.NotificationPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notifications" Width="340" Height="400"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Deactivated="Window_Deactivated">
    <Border Background="#EE1E1E2E" CornerRadius="12" Margin="6"
            BorderBrush="#FF3A86FF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Notifications" FontSize="16" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,10"/>

            <ListBox x:Name="LstNotifications" Grid.Row="1"
                     Background="Transparent" BorderBrush="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FF2A2A3E" CornerRadius="8" Padding="10" Margin="0,0,0,6" Width="290">
                            <StackPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                                               Foreground="White" FontSize="13"/>
                                    <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}"
                                               Foreground="#FF888888" FontSize="11"
                                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </DockPanel>
                                <TextBlock Text="{Binding Message}" Foreground="#FFCCCCCC" FontSize="12"
                                           TextWrapping="Wrap" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2" Content="Clear All" Click="BtnClear_Click"
                    HorizontalAlignment="Right" Margin="0,8,0,0"
                    Padding="14,6" Background="#FF555577" Foreground="White" BorderBrush="Transparent"/>
        </Grid>
    </Border>
</Window>
